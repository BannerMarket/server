const ROUTES = {
    // Get all price requests or create new one
    priceRequests: '/price-request/requests',
    // Delete selected price request
    priceRequest: '/price-request/requests/:id'
};

module.exports = ROUTES;
