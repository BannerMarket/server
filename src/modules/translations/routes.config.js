const ROUTES = {
    // Get full dictionary for the particular language
    dictionary: '/dictionary/by-language',
    // Set translation in the database
    translation: '/dictionary/by-key/:key'
};

module.exports = ROUTES;
